# TypeScript Rules

- strict mode is on — no `any`, no type casting to escape type errors
- use interfaces for component props (not type aliases)
- no implicit returns in non-void functions
- do not suppress TypeScript errors with `@ts-ignore` or `@ts-expect-error` without a comment explaining why
